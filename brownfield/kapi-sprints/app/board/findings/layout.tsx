import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Findings · Kapi Sprints' }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
