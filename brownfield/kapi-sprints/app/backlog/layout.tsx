import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Backlog · Kapi Sprints' }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
