# Layer 7 — Eval

**Read these files:**
- `lib/manifest-transformer.ts` — search for `eval`, `criteria`, `evaluators`
- `lib/evals/` — what evaluator engines exist?
- `app/api/chat/route.ts` — is eval called on every response?

**Key question:** Is eval running automatically on chat responses, or only when explicitly triggered?
