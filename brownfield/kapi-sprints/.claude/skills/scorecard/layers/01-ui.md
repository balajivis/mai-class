# Layer 1 — UI

**Read these files:**
- `lib/manifest-transformer.ts` — search for `uiPattern`, `layout`, `theme`, `chatInterface`
- `components/patterns/PatternRenderer.tsx` (or wherever the 4 patterns are dispatched)
- `app/app/[tenantId]/page.tsx` — how does it consume `tenant.ui`?

**Key question:** Does the deployed app use the config-driven `uiPattern` and layout, or hardcoded defaults?
