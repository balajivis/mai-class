# Layer 8 — Observability

**Read these files:**
- `lib/observability/` — what's wired?
- `lib/langchain/` or Langfuse client — is it configured per tenant?
- `app/api/chat/route.ts` — are traces being sent?

**Key question:** Is Langfuse receiving traces for deployed tenant apps in production?
