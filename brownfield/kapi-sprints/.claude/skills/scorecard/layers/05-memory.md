# Layer 5 — Memory

**Read these files:**
- `lib/manifest-transformer.ts` — search for `memory`, `crossSession`, `userPreferences`
- `lib/memory/` — what runtime code exists?
- `app/api/chat/route.ts` — is memory loaded/saved per tenant config?

**Key question:** Is any memory system actually reading `tenant.memory` config at runtime?
